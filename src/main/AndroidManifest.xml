<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="jp.bugscontrol">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".ui.Application"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@android:style/Theme.Holo.Light.DarkActionBar">
        <activity
            android:name=".ui.ActivityServer"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.ActivityServerManager"
            android:label="@string/manage_servers" />
        <activity
            android:name=".ui.ActivityRegister"
            android:parentActivityName=".ui.ActivityServerManager" />
        <activity
            android:name=".ui.ActivityProduct"
            android:parentActivityName=".ui.ActivityServer" />
        <activity
            android:name=".ui.ActivityBug"
            android:label=""
            android:parentActivityName=".ui.ActivityProduct" />
    </application>

</manifest>
